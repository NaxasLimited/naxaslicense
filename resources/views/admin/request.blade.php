@extends('admin.layout') @section('content')<h1 class="text-2xl font-bold">Request {{$activationRequest->request_token_prefix}}…</h1><p>{{$activationRequest->normalized_domain}} — {{$activationRequest->status}}</p>@if($activationRequest->status==='pending')<form method="POST" action="{{route('admin.requests.approve',$activationRequest)}}">@csrf<select name="license_id">@foreach($licenses as $l)<option value="{{$l->id}}">{{$l->license_id}}</option>@endforeach</select><button class="p-2 bg-green-700 text-white">Approve</button></form><form method="POST" action="{{route('admin.requests.reject',$activationRequest)}}">@csrf<input name="reason" required placeholder="Safe reason"><button>Reject</button></form><form method="POST" action="{{route('admin.requests.expire',$activationRequest)}}">@csrf<button>Expire</button></form>@endif @endsection
